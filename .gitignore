__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Datasets and trained weights are kept locally because they are too large for GitHub.
PlantVillage/
data/raw/
data/balanced/
models/*.pth
models/*.pt
models/*.png
raspberry/deploy/models/*.pt

# Keep lightweight metadata if needed.
!models/*.json

.env
*.log
